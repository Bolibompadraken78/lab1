import org.junit.Before;
import org.junit.Test;

import java.awt.*;

import static org.junit.Assert.assertEquals;
/*
public class Testol {
    private Bil bil;

    @Before
    public void setUp() {
        bil = new Bil(4, 100, Color.RED, "TestCar");
    }

    @Test
    public void testInitialDirection() {
        assertEquals( 0, bil.dir);
    }

    @Test
    public void testTurnLeft() {
        bil.turnLeft();
        assertEquals( 1, bil.dir);

        bil.turnLeft();
        assertEquals( 2, bil.dir);

        bil.turnLeft();
        assertEquals( 3, bil.dir);

        bil.turnLeft();
        assertEquals(0, bil.dir);
    }

    @Test
    public void testTurnRight() {
        bil.turnRight();
        assertEquals( 3, bil.dir);

        bil.turnRight();
        assertEquals( 2, bil.dir);

        bil.turnRight();
        assertEquals( 1, bil.dir);

        bil.turnRight();
        assertEquals( 0, bil.dir);
    }

    @Test
    public void testMove() {
        bil.startEngine();
        bil.move();
        assertEquals( 0.1, bil.posY, 0.001);

        bil.turnRight(); // dir = 3 (left)
        bil.move();
        assertEquals( -0.1, bil.posX, 0.001);

        bil.turnLeft(); // dir = 0 (up)
        bil.move();
        assertEquals( 0.2, bil.posY, 0.001);
    }
}

 */
